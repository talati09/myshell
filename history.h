#ifndef HISTORY_H
#define HISTORY_H

#include <string>
#include <vector>
using namespace std;

// reads input character by character
// handles arrow keys, backspace, history
string readInput(vector<string>& history);

#endif